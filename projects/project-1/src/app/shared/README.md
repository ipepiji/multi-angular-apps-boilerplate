# Shared Module

The shared module is designed to organize and streamline the code which any commonly used directives, pipes, and components will be put in one module(shared) and then import just that module(shared) in other parts of the app module.